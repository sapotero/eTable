"use strict";

/**
 * nCore Ядро nCore - загрузчик остальных модулей

 */
var nCore = nCore || {};
nCore.core = (function(){

  function init(){
    // console.log( this );
  };

  return {
    init: init
  };
})();