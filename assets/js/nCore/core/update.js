"use strict";

// resourse update

var nCore = nCore || {};
nCore.update = (function(){
  // обновление кода на живую
  var init = function(){

  };

  return {
    init: init
  }
})();
nCore.update.init();